name: debug_atttendify
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  camera: ^0.11.0+1
  dropdown_button2: ^2.3.9
  flutter:
    sdk: flutter
  flutter_secure_storage: ^9.2.2
  geolocator: ^12.0.0
  http: ^1.2.1
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/assets/
